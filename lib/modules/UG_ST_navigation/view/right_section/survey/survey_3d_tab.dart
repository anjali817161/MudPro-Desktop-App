import 'package:flutter/material.dart';

class Survey3DTab extends StatelessWidget {
  const Survey3DTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("3D View"));
  }
}
