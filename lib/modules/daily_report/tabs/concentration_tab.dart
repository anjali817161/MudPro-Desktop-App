import 'package:flutter/material.dart';

class ConcentrationTab extends StatelessWidget {
  const ConcentrationTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Concentration Tab Content'),
    );
  }
}
