import 'package:flutter/material.dart';

class SurveyTab extends StatelessWidget {
  const SurveyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Survey Tab Content'),
    );
  }
}
