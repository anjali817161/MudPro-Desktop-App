import 'package:flutter/material.dart';

class TotalCostTab extends StatelessWidget {
  const TotalCostTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Total Cost Tab Content'),
    );
  }
}
